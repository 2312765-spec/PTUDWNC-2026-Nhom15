# components/ui — người phụ trách: **C** (Sprint 0)

HỢP ĐỒNG CHUNG — cả 4 người dùng. Chốt tuần 1.

Cần có: `Button`, `Input`, `Textarea`, `Select`, `Card`, `Badge`, `Skeleton`,
`Toast`, `Dialog`, `Pagination`.

NFR-USE-004: mọi async operation phải có loading state (skeleton, không blank screen),
toast xác nhận sau write, rollback khi API fail.
NFR-USE-002: WCAG 2.1 AA — aria-label, điều hướng bàn phím (Tab/Enter/Escape), contrast >= 4.5:1.
